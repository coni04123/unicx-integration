<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to {{companyName}}</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.6;
            color: #222222;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }
        .email-wrapper {
            width: 100%;
            background-color: #f5f5f5;
            padding: 40px 0;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border: 1px solid #e0e0e0;
        }
        .header {
            padding: 30px 40px;
            border-bottom: 3px solid #14a800;
            background-color: #ffffff;
        }
        .logo {
            max-width: 150px;
            height: auto;
            display: block;
        }
        .content {
            padding: 40px;
        }
        .greeting {
            font-size: 18px;
            font-weight: 400;
            color: #222222;
            margin: 0 0 20px 0;
        }
        .message {
            font-size: 15px;
            color: #555555;
            line-height: 1.7;
            margin: 0 0 30px 0;
        }
        .qr-section {
            text-align: center;
            margin: 30px 0;
        }
        .qr-image {
            border: 2px solid #14a800;
            padding: 15px;
            background: white;
            border-radius: 4px;
            max-width: 250px;
            height: auto;
            margin: 20px auto;
            display: block;
        }
        .info-box {
            background-color: #f9f9f9;
            border: 1px solid #e0e0e0;
            border-left: 3px solid #14a800;
            border-radius: 4px;
            padding: 25px;
            margin: 30px 0;
        }
        .info-box-title {
            font-size: 14px;
            font-weight: 600;
            color: #222222;
            margin: 0 0 15px 0;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .info-list {
            font-size: 14px;
            color: #555555;
            line-height: 1.8;
            margin: 0;
            padding-left: 20px;
        }
        .info-list li {
            margin-bottom: 10px;
        }
        .warning-box {
            background-color: #f0f9f0;
            border-left: 3px solid #14a800;
            padding: 20px;
            margin: 30px 0;
        }
        .warning-box-title {
            font-size: 14px;
            font-weight: 600;
            color: #222222;
            margin: 0 0 10px 0;
        }
        .warning-list {
            font-size: 14px;
            color: #555555;
            line-height: 1.8;
            margin: 0;
            padding-left: 20px;
        }
        .warning-list li {
            margin-bottom: 8px;
        }
        .expiry-text {
            font-size: 13px;
            color: #666666;
            margin-top: 15px;
        }
        .divider {
            height: 1px;
            background-color: #e0e0e0;
            margin: 30px 0;
        }
        .footer {
            padding: 30px 40px;
            background-color: #f9f9f9;
            border-top: 1px solid #e0e0e0;
        }
        .footer-text {
            font-size: 13px;
            color: #666666;
            line-height: 1.6;
            margin: 0 0 15px 0;
        }
        .footer-links {
            margin: 20px 0;
        }
        .footer-links a {
            color: #14a800;
            text-decoration: none;
            font-size: 13px;
            margin-right: 20px;
        }
        .footer-links a:hover {
            text-decoration: underline;
        }
        .footer-company {
            font-size: 12px;
            color: #999999;
            margin-top: 20px;
            line-height: 1.6;
        }
        @media only screen and (max-width: 600px) {
            .email-container {
                width: 100% !important;
            }
            .header, .content, .footer {
                padding: 25px !important;
            }
            .qr-image {
                max-width: 200px !important;
            }
        }
    </style>
</head>
<body>
    <div class="email-wrapper">
        <div class="email-container">
            <!-- Header -->
            <div class="header">
                {{#if logoUrl}}
                <img src="{{logoUrl}}" alt="{{companyName}}" class="logo">
                {{else}}
                <div style="font-size: 24px; font-weight: 600; color: #14a800;">{{companyName}}</div>
                {{/if}}
            </div>

            <!-- Content -->
            <div class="content">
                <p class="greeting">Hi {{firstName}} {{lastName}},</p>
                
                <p class="message">
                    You've been invited to join the {{companyName}} platform. Scan the QR code below with WhatsApp to connect your account.
                </p>

                <!-- QR Code Section -->
                <div class="qr-section">
                    <img src="{{qrCodeImage}}" alt="WhatsApp QR Code" class="qr-image">
                    {{!-- <p class="expiry-text">QR Code expires in {{expiryHours}} hours</p> --}}
                </div>

                <!-- Instructions -->
                <div class="info-box">
                    <div class="info-box-title">Instructions</div>
                    <ol class="info-list">
                        <li>Open WhatsApp on your phone</li>
                        <li>Tap Menu or Settings and select WhatsApp Web</li>
                        <li>Point your phone at this screen to capture the QR code</li>
                        <li>Your WhatsApp account will be connected automatically</li>
                    </ol>
                </div>

                <!-- Important Notes -->
                <div class="warning-box">
                    <div class="warning-box-title">Important Notes</div>
                    <ul class="warning-list">
                        <li>This QR code is valid for 5 minutes only</li>
                        <li>Each QR code can only be used once</li>
                        <li>Make sure you're using the official WhatsApp application</li>
                    </ul>
                </div>

                {{#if message}}
                <div class="warning-box">
                    <div class="warning-box-title">Notice</div>
                    <p class="message" style="margin: 0;">{{message}}</p>
                </div>
                {{/if}}

                <div class="divider"></div>

                <p class="message" style="margin-bottom: 0;">
                    If you have any questions or need assistance, please contact our support team.
                </p>
            </div>

            <!-- Footer -->
            <div class="footer">
                <p class="footer-text">Best regards,<br>The {{companyName}} Team</p>
                
                <div class="footer-links">
                    {{#if socialLinks.website}}
                    <a href="{{socialLinks.website}}">Website</a>
                    {{/if}}
                    {{#if socialLinks.linkedin}}
                    <a href="{{socialLinks.linkedin}}">LinkedIn</a>
                    {{/if}}
                    {{#if socialLinks.twitter}}
                    <a href="{{socialLinks.twitter}}">Twitter</a>
                    {{/if}}
                </div>

                <div class="footer-company">
                    {{companyName}}<br>
                    {{companyAddress}}<br>
                    Support: <a href="mailto:{{supportEmail}}" style="color: #14a800;">{{supportEmail}}</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
